# House Lulu — Home Assistant (Docker: ./homeassistant → /config)

default_config:

homeassistant:
  name: House Lulu
  time_zone: Europe/Berlin
  currency: EUR
  country: DE
  packages: !include_dir_named packages

logger:
  default: warning
